import React from "react";
import { View } from "react-native";

const MyComponents = (props) => {
  if (props.hours === null || props.hours === undefined) return null;
};

export default MyComponents;
